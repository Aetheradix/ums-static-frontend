import TransportMenuPage from '../components/TransportMenuPage';

export default function CollegeMenuPage() {
  return (
    <TransportMenuPage
      slug="transport-college-login"
      sectionLabel="College Operations"
      sectionIcon="directions_bus"
      emptyMessage="No pages configured for College Login."
    />
  );
}
