services:
  ums-frontend:
    image: ums-frontend
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: ums_frontend
    ports:
      - '4003:443'
    logging:
      driver: 'k8s-file'
